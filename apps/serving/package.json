{
  "name": "serving",
  "version": "0.1.0",
  "private": true,
  "description": "MODEL-SERVE-002. Synchronous /predict serving process — loads PRODUCTION ModelVersions from object storage and answers bounded prediction requests. Separate image, separate lifecycle from images/trainer/ (decisions.serving_host_undecided, resolved 2026-09-02).",
  "scripts": {
    "dev": ".venv/bin/uvicorn main:app --reload --port 8100",
    "start": ".venv/bin/uvicorn main:app --host 0.0.0.0 --port 8100",
    "test": ".venv/bin/pytest",
    "lint": "echo 'apps/serving: no lint step yet'",
    "build": "echo 'apps/serving: no build step — image build handled by Dockerfile'"
  }
}
