# MODEL-SERVE-002. Pinned to MATCH images/trainer/requirements.txt exactly
# for the scikit-learn/pandas/numpy/joblib/lightgbm/xgboost lines — this
# process unpickles model.joblib objects the trainer image wrote, and a
# model pickled by one sklearn/pandas/numpy version cannot be trusted to
# unpickle correctly under a different one (see decisions.
# serving_transform_is_an_extracted_module and MODEL-SERVE-002-T02's own
# framework-version check). Do NOT bump these independently of
# images/trainer/requirements.txt — bump both together, in the same change.
pandas==2.2.3
pyarrow==17.0.0
scikit-learn==1.5.2
joblib==1.4.2
numpy==2.1.3
lightgbm==4.5.0
xgboost==2.1.2

# The softsensor_scaling package (packages/py-scaling/) — installed from
# the monorepo source at build/dev time, not from an index. See
# decisions.serving_transform_is_an_extracted_module.
-e ../../packages/py-scaling

# FastAPI serving surface. No PI/MinIO/SQL client libraries here on
# purpose — this process never touches a data source directly, only the
# descriptor endpoint (over plain requests) and a presigned URL.
fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
requests==2.32.3
pytest==9.1.1
# fastapi.testclient (TestClient) needs httpx at runtime — dev/test only,
# never imported by the serving app itself.
httpx==0.28.1
