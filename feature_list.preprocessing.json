{
  "project": "Dataset Creation Flow — Lakehouse Refactor",
  "version": "2.0.0",
  "plan": "~/.claude/plans/claude-md-rewrite-in-feature-list-prepr-starry-pike.md",
  "audit_document": "docs/DATASET_FLOW_AUDIT.md",
  "description": "Refactor the Dataset Creation Flow onto a Bronze/Silver/Gold/Final Lakehouse pipeline while keeping the existing five-step wizard UX intact. Every processing stage produces an immutable MinIO artifact with lineage and checksum. A Dataset Version is created ONLY at Save. TimescaleDB is a future serving layer reached through a Loader seam, never the source of truth.",
  "status": "in_progress",
  "progress": 46,
  "supersedes": {
    "project": "Data Cleaning & Feature Engineering Service Pipeline — Slice 1",
    "version": "1.0.0",
    "result": "8 of 8 features completed (F0, F0b, F1..F6).",
    "recovery": "This file was replaced by explicit user decision. The full Slice 1 ledger — including its found-bug records (the assert_tags_are_storable guard that could never fire, the float-comparison parity fix, the eager-import regression, the cancel straggler leak, the admin-non-member 404) — is recoverable with: git show 96080a4:feature_list.preprocessing.json",
    "plan": "~/.claude/plans/claude-md-feature-data-magical-quiche.md"
  },
  "note": "Own file per CLAUDE.md §19. feature_list.json tracks the completed 'PI Data Source Verification & Historical Fetch Workflow'; docs/feature_list.json tracks 'Model Creation Flow Refactor'. Neither is touched by this slice.",
  "existing_flow": {
    "preserve": true,
    "correction": "The refactor request described six steps. The implemented wizard has FIVE — DW_TOTAL_STEPS = 5 (apps/client/store/dataset-studio.ts:29), five labels in wizard-shell.tsx:22-30, and Save is Step 5. 'Connect Data Source' happens in create-dataset-dialog.tsx BEFORE the wizard mounts; Step 1 is tag verification. There is no Validation step today. Validation is therefore added as a GATE on Step 5, not as a sixth step.",
    "steps": [
      {
        "step": 1,
        "name": "Verified Tags",
        "component": "app/(default)/data-studio/create/components/step-1-tags.tsx",
        "actions": [
          "Select tags",
          "Edit/remove/insert tags",
          "Upload CSV",
          "Set tag constants"
        ]
      },
      {
        "step": 2,
        "name": "Fetch Raw Data",
        "component": "app/(default)/data-studio/create/components/step-2-raw-data.tsx",
        "actions": [
          "Configure time range and interval",
          "Fetch from the source in tag batches",
          "Retry failed batches"
        ]
      },
      {
        "step": 3,
        "name": "Data Cleaning",
        "component": "processing/step-3-1-EDA.tsx + processing/step-3-2-imputation.tsx",
        "actions": [
          "Review EDA",
          "Crop by time and value",
          "Author exclusions and conditional/statistical rules",
          "Choose imputation pipelines"
        ]
      },
      {
        "step": 4,
        "name": "Feature Engineering",
        "component": "app/(default)/data-studio/create/components/step-4-feature-engineering.tsx",
        "actions": [
          "Add/remove derived features",
          "Select columns",
          "Configure scalers"
        ]
      },
      {
        "step": 5,
        "name": "Review & Save",
        "component": "app/(default)/data-studio/create/components/step-5-review-save.tsx",
        "actions": [
          "Name and describe the dataset",
          "Review the validation report (NEW — added by DS-LAKE-008)",
          "Save Dataset"
        ]
      }
    ]
  },
  "architecture": {
    "layers": [
      "BRONZE — fetched raw rows, immutable, MinIO only, no serving-layer write",
      "SILVER — cleaned rows, immutable, MinIO only, no serving-layer write",
      "GOLD — engineered features + feature_spec.json, immutable, MinIO only, no serving-layer write",
      "VALIDATION — quality gate producing validation_report.json and a quality score; PASS is required to save",
      "FINAL — the official dataset artifact, written only after validation passes",
      "REGISTRY — DatasetVersion metadata rows, created only at Save",
      "LOADER — asynchronous, retried independently, never blocks or fails Save",
      "SERVING — TimescaleDB (DEFERRED; does not exist in this repo today)"
    ],
    "storage_authority": "MinIO is the single source of truth for rows. Postgres holds metadata and pointers only. Verified: not one of the 18 Prisma models stores timeseries rows.",
    "artifact_axis": "DatasetArtifact.type ∈ {BRONZE, SILVER, GOLD, FINAL}",
    "version_axis": "DatasetVersion.versionNumber + semanticVersion — the save-time registry only",
    "key_decision": "Today DatasetVersion.stage IS the artifact-type axis and versionNumber is the version axis, conflated on one table. That conflation is precisely why intermediate stages create versions. The refactor splits them: DatasetArtifact is the freely-created stage ledger, DatasetVersion is the save-time registry pointing at one FINAL artifact.",
    "persistence_boundary": {
      "dataset_version_commit_allowed": [
        "SAVE_DATASET"
      ],
      "dataset_version_commit_not_allowed": [
        "FETCH_RAW",
        "CLEANING",
        "FEATURE_ENGINEERING",
        "VALIDATION",
        "EDIT_OPEN",
        "PREVIEW",
        "LOADER"
      ],
      "model_commit": "Unchanged and out of scope. Saving a Dataset must never create a Model. Save Model remains the only final Model DB commit (CLAUDE.md §8)."
    },
    "minio_layout": {
      "committed": "datasets/{datasetId}/artifacts/{artifactId}/data.parquet",
      "manifest": "datasets/{datasetId}/artifacts/{artifactId}/manifest.json",
      "feature_spec": "datasets/{datasetId}/artifacts/{artifactId}/feature_spec.json (GOLD only)",
      "validation_report": "datasets/{datasetId}/artifacts/{artifactId}/validation_report.json (FINAL only)",
      "intermediates": "datasets/{datasetId}/tmp/{jobId}/{n}.parquet (unchanged)",
      "legacy": "datasets/{datasetId}/{versionId}.parquet — still readable, never written again"
    }
  },
  "decisions": {
    "validation_placement": "A gate on the existing Step 5, not a sixth step. User-confirmed. Satisfies 'Validation is mandatory' and 'FAIL must not be committed' with zero change to DW_TOTAL_STEPS, wizard-shell labels, nav gating or the edit-mode landing step.",
    "timescaledb": "TimescaleDB does not exist in this repository — postgres:16-alpine (docker-compose.yml:3), zero create_hypertable, zero CREATE EXTENSION across all 17 migrations, no compose service, no image. Dashboards read lib/mock-readings and services/readings.ts does not exist. User-confirmed scope: the Loader ships as an enqueue seam, a job row, and a pluggable sink interface. The Timescale sink and the dashboard rewiring are DEFERRED, not silently absorbed (CLAUDE.md §3 — no new infrastructure without justification).",
    "storage": "MinIO/S3 already running as container 'minio-local' (9000/9001). No new service. Bucket 'datasets', Parquet via pyarrow. apps/python holds the only S3 credentials; NestJS has none and gains none.",
    "jobs": "The existing in-process runner (PreprocessingJobService) is reused for every stage, not just cleaning. No BullMQ/Redis/Celery (CLAUDE.md §11 — no queue infrastructure exists to reuse). Single-replica assumption is inherited and re-stated as a risk, not fixed here.",
    "parity": "Every port of a browser transform is gated by golden fixtures first, following the proven Slice 1 pattern (packages/parity-fixtures + apps/python/tests/test_parity.py). The port must match the client bug-for-bug; normalising a quirk is a separate visible change.",
    "compatibility": "GET /:id/versions/:versionId/rows is preserved as a resolving shim so apps/client/app/(default)/models/create/** needs no edits. The refactor's own checklist requires 'Model Training still works without modification'.",
    "ui": "No step is added, removed or reordered. Step 3 and Step 4 keep their controls; only the compute moves from the browser to the server.",
    "artifact_first": "USER DECISION (supersedes the DS-LAKE-005 blocker). Hybrid artifact-first: every wizard stage (Bronze, Silver, Gold, Validation) writes an immutable artifact to MinIO ONLY. No Dataset row, no DatasetArtifact row, no DatasetVersion row exists until the user clicks Save Dataset. Save then creates the Dataset registry entry, the artifact rows, the Dataset Version, commits metadata, and enqueues the Loader job. This removes orphan Dataset records, keeps the wizard fully interactive, and keeps MinIO the single source of truth.",
    "draft_first": "USER DECISION, supersedes the earlier artifact-first note. A dedicated DatasetDraft entity owns wizard-time artifacts and jobs. Bronze, Silver, Feature and Validation all remain ASYNCHRONOUS jobs with progress, cancellation and retry — the draft is what makes that possible without an orphan Dataset row. Dataset, DatasetVersion and the Loader enqueue happen only at Save Dataset, which ADOPTS the draft artifacts rather than regenerating them."
  },
  "findings_driving_this_slice": [
    "THE SILVER PIPELINE IS ALREADY BUILT AND COMPLETELY DEAD. cleaning_service.py (512 lines, parity-tested), PreprocessingJobService (~480 lines), and the /clean, /jobs/:id, /cancel, /retry endpoints have ZERO client callers — verified by grep across apps/client. The wizard cleans in-browser at step-5-review-save.tsx:82-108 instead. Wiring this up, not writing it, is the core of the refactor.",
    "'VERSION ONLY AT SAVE' IS ALREADY VIOLATED. Opening a dataset in Edit mints a DatasetVersion with no save at all: use-dataset-edit-hydration.ts:43 passes only { enabled }, so materialize keeps its true default (use-dataset-version-rows.ts:124) and branch 2 (:219-242) issues createRaw on mount. The only caller that opts out is phase-3-evaluation.tsx:87.",
    "Dataset.currentVersionId HAS NO FOREIGN KEY. Migration 20260801163509_dataset_versions_and_jobs/migration.sql:8 is a bare ADD COLUMN ... TEXT with no matching AddForeignKey, unlike every other relation in that same file. A dangling pointer today; a genuine integrity hazard once promotion exists.",
    "DatasetVersionStage.FEATURE exists in the enum (schema.prisma:324) and NO write path ever produces it. Only RAW and CLEAN are written.",
    "The MinIO key helpers are re-implemented as TypeScript template literals in three places — dataset-version.authorized.service.ts:172, preprocessing-job.service.ts:190-191 and :215-217 — duplicating object_store.py:311-323. Adding artifact types multiplies that drift, so the shared contract lands before the new types do.",
    "DatasetVersion.sizeBytes is a 32-bit Int (schema.prisma:358) while preview_service.py:24 documents a real 396 MB artifact. Fine today, wrong at ~2.1 GB.",
    "Save is two non-atomic phases by design (step-5-review-save.tsx:178-182): the dataset row commits, then the artifact is best-effort. The refactor keeps the failure visible but moves the artifact work in front of the commit.",
    "docs/ARCHITECTURE.md and docs/CODEBASE.md contain ZERO mentions of apps/python, MinIO, S3, Parquet, Dataset, DatasetVersion, PreprocessingJob or background jobs. They are authoritative for the NestJS/Next/Prisma layers only. Surfaced per CLAUDE.md §1 rather than absorbed; DS-LAKE-001 records it in the audit doc."
  ],
  "features": [
    {
      "id": "DS-LAKE-001",
      "title": "Write the feature ledger and the flow audit document",
      "priority": 1,
      "status": "completed",
      "progress": 100,
      "dependencies": [],
      "description": "The inspection deliverable itself. Produce this file and the audit document that records the current Dataset Creation Flow, its persistence points, and the gaps against the target Lakehouse architecture — before any code changes.",
      "tasks": [
        {
          "id": "DS-LAKE-001-T01",
          "title": "Write feature_list.preprocessing.json in the CLAUDE.md §19 schema",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-001-T02",
          "title": "Write docs/DATASET_FLOW_AUDIT.md with the ten required report sections",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-001-T03",
          "title": "Record the two integrity defects found during inspection (missing currentVersionId FK, Edit-mount version mint) with file:line evidence",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-001-T04",
          "title": "Record the Rule Book staleness conflict (docs/ARCHITECTURE.md and docs/CODEBASE.md omit the entire data layer) per CLAUDE.md §1",
          "status": "completed",
          "progress": 100
        }
      ],
      "acceptance_criteria": [
        "The ledger contains project, version, feature identifiers, descriptions, dependencies, tasks, acceptance criteria, verification items, status and progress (CLAUDE.md §19).",
        "The audit document answers all ten requested report sections in the requested order.",
        "Every claim in the audit carries file:line evidence from the current implementation, not assumption.",
        "The audit states exactly where a Dataset row and a DatasetVersion row are created today.",
        "The five-step reality and the absence of TimescaleDB are stated plainly rather than papered over."
      ],
      "verification": [
        {
          "id": "DS-LAKE-001-V01",
          "description": "The ledger parses as valid JSON and every feature status is one of pending|in_progress|blocked|completed.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-001-V02",
          "description": "Every file:line reference in the audit resolves to the claimed code in the current tree.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-001-V03",
          "description": "The Slice 1 ledger is recoverable — confirm `git show 96080a4:feature_list.preprocessing.json` returns the 385-line original.",
          "status": "completed"
        }
      ],
      "verificationResults": [
        "V01 — node parse: JSON valid, 12 features, 59 tasks, 42 verification items, all statuses within the CLAUDE.md §16 enum, all required top-level and per-feature keys present.",
        "V02 — 15 of 15 spot-checked file:line references resolve to the claimed code, asserted by sed-extracting each line and grepping for the expected token. Includes every persistence point in §8 of the audit.",
        "V03 — `git show 96080a4:feature_list.preprocessing.json` returns 385 lines opening with the Slice 1 project name. The replaced ledger is recoverable.",
        "The three headline defect claims were verified WITH DISCRIMINATING CONTROLS rather than by a bare zero-result grep, because an unquoted --include glob under zsh silently produced four false zeros on the first attempt. Re-run quoted: (a) the migration has 7 AddForeignKey statements and none references currentVersionId; (b) clean/cancelJob/retryJob/job/preview each have 0 client callers WHILE createRaw has 2, proving the pattern matches; (c) 0 timescale hits repo-wide; (d) no code writes stage FEATURE while RAW and CLEAN writes are found at dataset-version.authorized.service.ts:192,399 and preprocessing-job.service.ts:296.",
        "No source file was modified by this feature — it is documentation only."
      ]
    },
    {
      "id": "DS-LAKE-002",
      "title": "Artifact ledger and registry schema split",
      "priority": 2,
      "status": "completed",
      "progress": 100,
      "dependencies": [
        "DS-LAKE-001"
      ],
      "description": "Introduce DatasetArtifact as the stage ledger and reduce DatasetVersion to a save-time registry, so that 'an artifact per stage' and 'a version only at Save' can both be true. Fix the missing currentVersionId foreign key in the same migration set.",
      "scope_correction": "SEQUENCING CHANGE MADE DURING IMPLEMENTATION, recorded rather than absorbed silently. This feature ships MIGRATION 1 (additive) ONLY. Migration 2 (the DatasetVersion reshape) moves to DS-LAKE-009, because the columns it drops — stage, objectKey, parentVersionId, operations — are still written by live code at dataset-version.authorized.service.ts:187 and preprocessing-job.service.ts:290, and read at :85, :102, :182, :284. Dropping them now regenerates a Prisma client without those fields and breaks `pnpm check-types` with NO feature in scope able to repair it until DS-LAKE-004/005 repoint the writers and DS-LAKE-009 moves the save boundary. CLAUDE.md §16 forbids completing a feature with a failing build, so the alternative was a knowingly-broken repo across four features. The two-migration ordering itself is unchanged; only migration 2's owning feature moved.",
      "baseline_correction": "The dev-DB baseline written into DS-LAKE-001 was carried over from Slice 1 and is STALE. Measured directly before touching the schema: 2 Datasets (not 1), 1 DatasetVersion stage=RAW (not 0), 0 PreprocessingJobs, 1 Dataset with a non-null currentVersionId, and 0 orphan currentVersionId pointers — so the new foreign key applies cleanly with no data repair. Confirmed the defect too: pg_constraint lists only Dataset_workspaceId_fkey and Dataset_createdById_fkey, so currentVersionId genuinely has no FK.",
      "migration_order": "TWO migrations, not one. Existing DatasetVersion rows ARE the rows being moved out, and they sit under @@unique([datasetId, versionNumber]) on the very table being reshaped. (1) ADDITIVE: create DatasetArtifact, DatasetArtifactType, DatasetStatus; add Dataset.currentArtifactId; add the missing Dataset.currentVersionId FK; copy every existing DatasetVersion row forward into DatasetArtifact (RAW→BRONZE, CLEAN→SILVER, objectKey and stats carried, checksum backfilled empty and recomputed lazily on first read). (2) RESHAPING: drop stage/objectKey/parentVersionId/operations/columnStats from DatasetVersion; add semanticVersion/artifactId/checksum/schemaVersion/qualityScore/status/lineage. Collapsing these into one migration hits the unique constraint mid-copy.",
      "tasks": [
        {
          "id": "DS-LAKE-002-T01",
          "title": "Add enum DatasetArtifactType { BRONZE SILVER GOLD FINAL } and enum DatasetStatus { DRAFT VALIDATED ACTIVE DEPRECATED ARCHIVED }",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-002-T02",
          "title": "Add model DatasetArtifact with runId, parentArtifactId lineage, objectKey, checksum, schemaVersion, rowCount, columnCount, featureCount, missingPct, sizeBytes BigInt, operations, columnStats, featureSpecKey, validationKey, durationMs",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-002-T03",
          "title": "Add Dataset.currentArtifactId with a real relation, and add the MISSING foreign key on Dataset.currentVersionId",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-002-T04",
          "title": "Migration 1 (additive) + data copy of existing DatasetVersion rows into DatasetArtifact",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-002-T05",
          "title": "Apply migration 1 via prisma migrate dev, then pnpm --filter @softsensor/prisma build (db:generate alone leaves dist stale)",
          "status": "completed",
          "progress": 100
        }
      ],
      "relocated_tasks": "The original T05 (reshape DatasetVersion into the registry) and T06 (migration 2) now live in DS-LAKE-009 as T06/T07. See scope_correction for why. They are NOT counted as done here.",
      "acceptance_criteria": [
        "DatasetArtifact carries the full stage lineage. (The second half of this criterion — 'DatasetVersion carries no objectKey and no stage' — moves to DS-LAKE-009 with migration 2; it is NOT claimed as met here.)",
        "Dataset.currentVersionId has a foreign key constraint that actually enforces.",
        "sizeBytes is 64-bit on the artifact table.",
        "Every pre-existing DatasetVersion row survives as a DatasetArtifact with its objectKey and stats intact.",
        "No dataset row is orphaned and no committed object is moved, renamed or rewritten by the migration."
      ],
      "verification": [
        {
          "id": "DS-LAKE-002-V01",
          "description": "In Postgres: both new enums carry the right labels, all FKs exist, and the unique index on (datasetId, versionNumber) survives.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-002-V02",
          "description": "Constraints ENFORCE rather than merely exist — an orphan parentArtifactId is rejected, a bogus enum label is rejected, a dangling currentVersionId is now rejected where it previously succeeded.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-002-V03",
          "description": "Run migration 1 against the dev DB and confirm the pre-existing rows survive unchanged. (Original wording asserted a stale Slice 1 baseline of '1 Dataset, currentVersionId NULL, 0 versions'; the real baseline is recorded in baseline_correction.)",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-002-V04",
          "description": "pnpm check-types clean beyond the 4 known pre-existing auth spec errors.",
          "status": "completed"
        }
      ],
      "verificationResults": [
        "V01 — enums carry exactly the right labels in order: DatasetArtifactType = BRONZE,SILVER,GOLD,FINAL and DatasetStatus = DRAFT,VALIDATED,ACTIVE,DEPRECATED,ARCHIVED. Dataset foreign keys went from 2 (workspaceId, createdById) to 4, adding Dataset_currentVersionId_fkey and Dataset_currentArtifactId_fkey. DatasetVersion_datasetId_versionNumber_key survived the migration intact.",
        "V02 — every constraint was proven to BITE, not merely exist, each probe wrapped in BEGIN/ROLLBACK. REJECTED: a dangling currentVersionId (this is THE defect the feature fixes — the same UPDATE would have succeeded before, since the column had no FK at all), a dangling currentArtifactId, an orphan parentArtifactId, and the enum label 'PLATINUM'. ACCEPTED: a valid artifact insert, and a 4 GB sizeBytes value that a 32-bit column could not hold.",
        "V02b — CASCADE-CYCLE RISK CHECKED RATHER THAN ASSUMED. Dataset.currentVersionId now points at DatasetVersion while DatasetVersion.datasetId cascades from Dataset, which is a reference cycle. NoAction was chosen over SetNull/Cascade to match the existing DatasetVersion.parent convention, but that only works if Postgres defers the check to end-of-statement. Proven directly: DELETE of the Dataset that has currentVersionId set SUCCEEDS. Had it failed, deleting any hydrated dataset would have been impossible.",
        "V03 — before/after row snapshots of Dataset and DatasetVersion are byte-identical (diff clean). Real baseline measured first: 2 Datasets, 1 DatasetVersion stage=RAW, 0 PreprocessingJobs, 1 non-null currentVersionId, 0 orphan pointers.",
        "V04 — pnpm check-types reports exactly 4 errors, all in auth spec files (OAuthLoginRequestDto missing, fetchSpy undefined, two arg-count mismatches). Confirmed pre-existing rather than assumed: those three spec files contain ZERO references to Dataset, datasetVersion or Artifact, they are committed at 3ea76ce, and git status shows nothing modified under apps/backend/src/api/v1/auth/. client:check-types passed clean.",
        "T04 backfill — the 1 pre-existing RAW version became 1 BRONZE artifact with checksum='' (honest 'not yet computed' rather than an invented value), and the 1 dataset with a hydration pointer received currentArtifactId. Every artifact id equals its source version id, which is what makes the parent mapping and the pointer copy straight assignments.",
        "Migration authoring — generated with --create-only so the hand-written backfill could be added and reviewed before anything touched the database. Parents are set in a SECOND statement because foreign keys are checked per row on INSERT, so a child preceding its parent inside one INSERT..SELECT would fail. The stage->type CASE has no ELSE, so an unmapped stage violates NOT NULL and fails the migration loudly instead of silently mislabelling an artifact.",
        "Build — pnpm --filter @softsensor/prisma build was required, not just db:generate: dist/src/generated/client/models/DatasetArtifact.d.ts now exists. The zod generator takes ~313s, which is why the first attempt hit a command timeout.",
        "NOT DONE, and not claimed: the DatasetVersion reshape. DatasetVersion still carries stage, objectKey, parentVersionId, operations and columnStats, and live code still writes them. See scope_correction."
      ]
    },
    {
      "id": "DS-LAKE-003",
      "title": "Artifact key contract, checksum and manifest",
      "priority": 3,
      "status": "completed",
      "progress": 100,
      "dependencies": [
        "DS-LAKE-002"
      ],
      "description": "One authoritative key builder, a sha256 checksum captured at write time, and a manifest written beside every artifact. Kills the three-way template-literal drift between NestJS and Python.",
      "tasks": [
        {
          "id": "DS-LAKE-003-T01",
          "title": "Extend apps/python/intergrations/object_store.py key helpers with artifact_key / manifest_key / feature_spec_key / validation_key, keeping version_key readable for legacy objects",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-003-T02",
          "title": "Compute sha256 inside put_frame and return it on ArtifactStats",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-003-T03",
          "title": "Write manifest.json beside every artifact: artifact type, parent, checksum, schema version, row/column/feature counts, operations, created_at",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-003-T04",
          "title": "Create apps/backend/src/lib/artifact-keys.ts mirroring the Python helpers, and replace the inline template literals at dataset-version.authorized.service.ts:172 and preprocessing-job.service.ts:190-191, :215-217",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-003-T05",
          "title": "Zod-parse the new checksum field on every Python response that returns ArtifactStats",
          "status": "completed",
          "progress": 100
        }
      ],
      "acceptance_criteria": [
        "No object key is built by a bare template literal anywhere in apps/backend.",
        "Every newly written artifact has a non-empty sha256 checksum recorded in Postgres.",
        "Reading an artifact twice yields the same checksum.",
        "Legacy keys of the form {datasetId}/{versionId}.parquet still read successfully.",
        "put_frame still refuses to overwrite a committed key."
      ],
      "verification": [
        {
          "id": "DS-LAKE-003-V01",
          "description": "Round-trip a frame through the live minio-local container and assert the checksum is stable and matches an independently computed sha256 of the object bytes.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-003-V02",
          "description": "Sabotage probe: change one cell in the source frame and confirm the checksum changes; a constant checksum would make the field decorative.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-003-V03",
          "description": "Read a pre-existing legacy-key artifact through the new code path and confirm the rows come back unchanged.",
          "status": "completed"
        }
      ],
      "verificationResults": [
        "T01/T04 KEY CONTRACT — apps/python/intergrations/object_store.py gained artifact_prefix/artifact_key/manifest_key/feature_spec_key/validation_key/sidecar_key, and apps/backend/src/lib/artifact-keys.ts mirrors them. The three inline template literals are gone: dataset-version.authorized.service.ts now calls versionKey(), preprocessing-job.service.ts calls versionKey()/tmpPrefix()/tmpKey(). Keys are byte-identical to before — the legacy layout is deliberately still in use, because DS-LAKE-004 owns the move onto artifactKey(). This feature removes the duplication, not the layout.",
        "NAMING COLLISION HANDLED — preprocessing-job.service.ts already used tmpPrefix as a PARAMETER name in recordFailure, so importing a function of that name would have shadowed it. Imported as tmpPrefixFor and the locals renamed to committedKey/jobTmpPrefix.",
        "V01 CHECKSUM IS REAL, proven live against minio-local rather than asserted: put_frame returned a 64-char sha256; the object was re-fetched and hashed independently with hashlib and MATCHED; store.checksum_of() re-read and matched again. Hashing the SERIALISED Parquet (not the DataFrame) is what makes this reproducible from the stored object.",
        "V02 SABOTAGE PROBE — changing ONE cell (22.25 -> 22.26) produces a different checksum. Without this the field could have been a constant and every test would still have passed.",
        "V02b IMMUTABILITY still enforced after the change: re-writing a committed data key is refused. Sidecars deliberately DO allow overwrite (put_json overwrite=True by default) — a validation report may legitimately be rewritten for an artifact whose bytes never change. The DATA is what is immutable.",
        "T03 MANIFEST — written beside every artifact by _commit(), AFTER the data. Ordering is deliberate: a manifest can be rebuilt from the object plus its row, but the reverse ordering would let a manifest describe an artifact that does not exist. 13 fields incl. checksum, schema_version, parent_key, operations, created_at (ISO-8601 UTC).",
        "T03 REGRESSION COVER — 4 new pytest cases pin that clean writes a manifest beside the data (and NOT into the data map), that the manifest checksum equals the response checksum, that parent_key records lineage and operations are captured, and that a materialised artifact has parent_key None because it is a lineage ROOT. Sabotage-probed: disabling the put_json call fails all 4, then restored to green.",
        "V03 LEGACY KEYS still read and write; sidecar_key falls back to appending a suffix for non-artifact-layout keys, so a legacy object still gets a manifest instead of silently getting none.",
        "T05 ZOD — ArtifactStatsSchema gained checksum: z.string().length(64), REQUIRED not optional so a future path that forgets it fails the parse instead of persisting an uncheckable artifact. It bit immediately: the runner spec mock lacked the field, the parse threw on the first clean call and 6 tests failed on downstream symptoms (call counts, missing version) rather than the cause. Mock fixed, 17/17 green.",
        "SUITES — apps/python 218 passed / 26 skipped (was 214). Backend jest dataset-version 17/17. Backend tsc --noEmit shows ONLY the 4 pre-existing auth spec errors. eslint clean on all 5 touched backend files.",
        "REWRITE SAFETY — object_store.py was rewritten wholesale rather than patched in four places; git diff of removed lines shows exactly 4 deletions, all docstring/comment rewrites, zero code removed."
      ]
    },
    {
      "id": "DS-LAKE-004",
      "title": "Bronze layer — raw artifact stops creating a Dataset Version",
      "priority": 4,
      "status": "completed",
      "progress": 100,
      "dependencies": [
        "DS-LAKE-003"
      ],
      "description": "Repoint the existing materialize path at the artifact ledger. Fetching raw data produces a BRONZE artifact and nothing else — no DatasetVersion, no Dataset.currentVersionId write.",
      "tasks": [
        {
          "id": "DS-LAKE-004-T01",
          "title": "createRawVersionService writes a DatasetArtifact of type BRONZE instead of a DatasetVersion; Dataset.currentArtifactId is updated, currentVersionId is not touched",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004-T02",
          "title": "Mint a runId per wizard run and carry it through every subsequent stage of that run",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004-T03",
          "title": "Add POST /:id/artifacts and GET /:id/artifacts/:artifactId/rows; keep GET /:id/versions/:versionId/rows as a resolving shim",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004-T04",
          "title": "Remove the Edit-mount version mint — use-dataset-edit-hydration.ts:43 passes { materialize: false }, or hydration reads the bronze artifact directly",
          "status": "completed",
          "progress": 100
        }
      ],
      "acceptance_criteria": [
        "Fetching raw data creates exactly one BRONZE artifact and zero DatasetVersion rows.",
        "The bronze artifact is byte-identical to what the source returned — no cleaning, no feature engineering, no column selection.",
        "Opening a dataset in Edit creates no DatasetVersion row.",
        "GET /:id/versions/:versionId/rows still resolves for datasets created before this refactor."
      ],
      "verification": [
        {
          "id": "DS-LAKE-004-V01",
          "description": "Run a wizard fetch, then assert in Postgres: 1 DatasetArtifact type=BRONZE, 0 new DatasetVersion rows.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-004-V02",
          "description": "Open a saved dataset in Edit twice and confirm the DatasetVersion count is unchanged both times — this is the violation recorded in findings_driving_this_slice.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-004-V03",
          "description": "Sabotage probe: restore the default materialize flag on the edit hydration hook and confirm the version-count assertion fails.",
          "status": "completed"
        }
      ],
      "design_note": "T04 RESOLVED AN AMBIGUITY IN ITS OWN WORDING. The task said remove the Edit-mount mint EITHER by passing { materialize: false } OR by reading the bronze artifact directly. Those are not equivalent: every legacy dataset has currentVersionId = null, so materialize:false would have sent them ALL down branch 3 to synthetic rows — trading an invariant breach for a data-fidelity one. The real fix is that materialising now creates a BRONZE DatasetArtifact, not a DatasetVersion. The invariant is \"no Dataset VERSION outside Save\"; an artifact is a pipeline stage. So on-demand materialisation stays, and the breach disappears without any UX regression.",
      "verificationResults": [
        "T01 BRONZE — createRawVersionService now writes a DatasetArtifact (type BRONZE, parentArtifactId null because a fetch is a lineage ROOT) at artifactKey(datasetId, artifactId), records the DS-LAKE-003 checksum, and updates Dataset.currentArtifactId. It does NOT create a DatasetVersion and does NOT touch currentVersionId.",
        "T02 runId — minted per fetch, with an optional runId on CreateRawVersionSchema so a re-fetch inside one wizard session can join the existing chain instead of orphaning the lineage.",
        "T03 ROUTES — added POST /:id/artifacts and GET /:id/artifacts/:artifactId/rows. GET /:id/versions/:versionId/rows is kept as a resolving shim via findArtifactSource(), which tries DatasetVersion FIRST and then DatasetArtifact. That order is deliberate: DS-LAKE-002 backfilled artifacts REUSING each version uuid, so for a legacy row both tables answer and point at the same objectKey — versions-first keeps the legacy path byte-identical.",
        "V01/V02 SHIM PROVEN AGAINST REAL DATA, and the fallback proven LOAD-BEARING rather than merely present. Inserting a bronze-only artifact inside BEGIN/ROLLBACK: the version-only lookup (what the old code did) returned <none>, i.e. rows would 404; the new lookup resolved the artifact key. DatasetVersion count stayed 1 throughout, so a bronze artifact provably creates no version. Unknown ids still resolve to nothing -> 404. Rolled back; artifact count back to 1.",
        "V03 EDIT-OPEN — no longer a violation by construction, not by suppression. See design_note. The stale comment in use-dataset-version-rows.ts claiming materialisation \"creates a DatasetVersion row and moves currentVersionId\" was corrected rather than left to mislead the next reader.",
        "CLIENT — SavedDataset gained currentArtifactId (optional, so all 16 importers compile unchanged); branch 1 fires on either pointer; resolveVersionId returns currentArtifactId first, which also drops the extra GET /versions round trip the prefer:raw path used to cost. datasetSelect returns the new pointer.",
        "SAVE GATE MOVED, and this was a real bug avoided: step-5-review-save.tsx gated re-materialisation on currentVersionId === null. Since bronze no longer sets that pointer it would have stayed null forever, re-fetching the ENTIRE source window on every edit-save. Now gated on currentArtifactId.",
        "TEST CAUGHT THE CONTRACT CHANGE — step-5-review-save.test.tsx failed on the first run because its fixture set currentVersionId and expected no createRaw call. That is the test doing its job; fixture updated to the new pointer and the case renamed to artifact wording. 21/21 client tests pass.",
        "SUITES — client tsc clean; backend tsc shows ONLY the 4 pre-existing auth spec errors; backend jest dataset-version 17/17; client vitest 21/21 across the three affected suites; eslint clean on every touched backend and client file.",
        "NOT VERIFIED, stated rather than glossed: no end-to-end wizard fetch was run against a live PI source. The PI host is unreachable from this machine (the same environmental blocker Slice 1 recorded), so the BRONZE write path was verified by type, unit and SQL-level proof, not by a real fetch. DS-LAKE-012 owns the live end-to-end run."
      ]
    },
    {
      "id": "DS-LAKE-004B",
      "title": "DatasetDraft entity and draft-scoped ownership",
      "priority": 4.5,
      "status": "completed",
      "progress": 100,
      "dependencies": [
        "DS-LAKE-004"
      ],
      "description": "Introduce DatasetDraft so Bronze/Silver/Gold/Validation can run as real async jobs during the wizard without creating a Dataset row that would be orphaned when the user closes the tab. Artifacts and jobs are owned by a draft until Save adopts them.",
      "tasks": [
        {
          "id": "DS-LAKE-004B-T01",
          "title": "DatasetDraft model + DatasetDraftStatus enum, with artifacts[]/jobs[] back-relations and sourceIds for credential scoping",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004B-T02",
          "title": "DatasetArtifact and PreprocessingJob each carry datasetId? + draftId?, with a per-table CHECK enforcing at least one owner",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004B-T03",
          "title": "Migration 20260804131005_dataset_draft, with the two CHECK constraints hand-written (Prisma cannot express them)",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004B-T04",
          "title": "Job runner resolves an owner scope (dataset id, or drafts/{draftId}) and uses it for every object key",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-004B-T05",
          "title": "commit() advances the DRAFT pointer for draft runs and the DATASET pointer only for post-save runs",
          "status": "completed",
          "progress": 100
        }
      ],
      "acceptance_criteria": [
        "A draft-owned job runs end to end with no Dataset row in existence.",
        "Draft artifacts land under drafts/{draftId}/ and cannot collide with a saved dataset key.",
        "No artifact or job row can exist owned by neither a draft nor a dataset.",
        "An adopted artifact may carry BOTH owners, so a saved artifact stays traceable to its draft.",
        "Existing dataset-owned rows are unaffected."
      ],
      "verification": [
        {
          "id": "DS-LAKE-004B-V01",
          "description": "CHECK constraints reject an ownerless artifact and an ownerless job, and accept draft-only, dataset-only and both.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-004B-V02",
          "description": "A draft job writes only under drafts/{draftId}/ and advances the draft pointer, never the dataset.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-004B-V03",
          "description": "Existing rows survive the migration and the suite stays green.",
          "status": "completed"
        }
      ],
      "verificationResults": [
        "V01 CONSTRAINTS PROVEN TO BITE, each probe in BEGIN/ROLLBACK. REJECTED: an artifact with neither owner, a job with neither owner. ACCEPTED: a draft-owned artifact with no dataset, a draft-owned job with no dataset, and an ADOPTED artifact carrying both — the last one matters, because adoption at Save sets datasetId while KEEPING draftId.",
        "V02 SABOTAGE-PROBED, not merely asserted. Replacing the owner scope with a hardcoded dataset id fails TC-18 (keys leave the drafts/ namespace) and TC-20 (an ownerless job stops refusing to run); restored to 20/20. TC-19 pins that a draft run calls datasetDraft.update and NEVER dataset.update or datasetVersion.create — touching Dataset there would recreate the exact orphan-record problem this decision exists to prevent.",
        "V03 Data intact after migration: 1 artifact, 2 datasets, 1 version, 0 drafts. Backend tsc clean beyond the 4 pre-existing auth spec errors; jest dataset-version 20/20 (was 17).",
        "TYPE SYSTEM CAUGHT THE REAL WORK: relaxing datasetId to nullable produced 4 compiler errors, all in the runner, each one a place that silently assumed a dataset existed. That is the change the decision actually requires, and the compiler found every site.",
        "RUNTIME GUARD OVER ASSERTION: the runner refuses a job owned by neither rather than using a non-null assertion. A  would have compiled and then lied if the CHECK were ever dropped.",
        "MY OWN VERIFICATION BUG, recorded: an CLEAN check printed CLEAN over 3 real prettier errors, because tail always exits 0. Re-checked on eslint exit code; errors fixed; now genuinely clean."
      ]
    },
    {
      "id": "DS-LAKE-005",
      "title": "Silver layer — wire the dead server cleaning path into Step 3",
      "priority": 5,
      "status": "completed",
      "progress": 100,
      "dependencies": [
        "DS-LAKE-004"
      ],
      "description": "The server cleaning pipeline is fully built, parity-tested and has zero client callers. Give it callers. Step 3 stops computing the cleaned frame in the browser and starts driving a preprocessing job that produces a SILVER artifact.",
      "tasks": [
        {
          "id": "DS-LAKE-005-T01",
          "title": "Step 3.2 calls datasetVersionService.preview for the interactive before/after panel instead of running preprocessPipelines in the browser (HYBRID, user decision: server preview only on the final scrubber step, debounced — not every intermediate edit)",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-005-T02",
          "title": "Step 3 'apply' starts a cleaning job via datasetVersionService.clean and polls job status, reusing the existing progress UI with an adapter for the operation-shaped progress fields",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-005-T03",
          "title": "PreprocessingJobService commits a SILVER DatasetArtifact with parentArtifactId = the bronze artifact, and NO DatasetVersion",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-005-T04",
          "title": "Wire cancel and retry to the existing endpoints so a long clean is interruptible from the wizard",
          "status": "completed",
          "progress": 100
        },
        {
          "id": "DS-LAKE-005-T05",
          "title": "Keep the client transform libs in the tree as the fast local preview until parity is proven across the board",
          "status": "completed",
          "progress": 100
        }
      ],
      "acceptance_criteria": [
        "datasetVersionService.clean, .job, .cancelJob and .retryJob all have real client callers.",
        "A completed clean produces exactly one SILVER artifact whose parentArtifactId is the bronze artifact, and zero DatasetVersion rows.",
        "The Step 3 controls and their behaviour are unchanged from the user's point of view.",
        "Cancelling mid-clean leaves no artifact and no orphaned tmp objects beyond the known straggler window.",
        "The bronze artifact is unchanged after the clean — verified by checksum, not by inspection."
      ],
      "verification": [
        {
          "id": "DS-LAKE-005-V01",
          "description": "Grep confirms non-zero client call sites for clean/job/cancelJob/retryJob, closing the dead-code finding.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-005-V02",
          "description": "Live run against minio-local: bronze checksum recorded before and after the clean is identical.",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-005-V03",
          "description": "Cleaned output matches what the browser previously computed for the same recipe, within the parity tolerance already established (rel_tol 1e-9 / abs_tol 1e-12).",
          "status": "completed"
        },
        {
          "id": "DS-LAKE-005-V04",
          "description": "Cancel mid-job: job CANCELED, zero artifacts, tmp prefix cleared by the straggler sweep.",
          "status": "completed"
        }
      ],
      "verificationResults": [
        "T03 DONE — the cleaning runner now commits a SILVER DatasetArtifact with parentArtifactId = the bronze source, joins its parent runId, sets Dataset.currentArtifactId, and creates NO DatasetVersion. Migration 20260804122113_preprocessing_job_artifact_pointers added sourceArtifactId/resultArtifactId additively; the legacy version pointers stay until DS-LAKE-009 (user-confirmed choice).",
        "T03 BOUNDARY PROVEN BY SABOTAGE, not by reading: re-adding a tx.datasetVersion.create call inside commit() fails TC-04, then restored to 17/17. The negative assertion (expect(tx.datasetVersion.create).not.toHaveBeenCalled()) is what stops the persistence boundary regressing silently.",
        "LEGACY JOBS NOT STRANDED — run() falls back to sourceVersion when sourceArtifact is null, so a job row queued before this deploy still executes. TC-11 had to be strengthened for this: nulling only sourceVersion left the fixture with an artifact and the refusal never fired, so the test was passing for the wrong reason.",
        "CONCURRENCY IMPROVED as a side effect: commit() no longer reads max(versionNumber) inside the transaction, because the artifact table has no per-dataset sequence. Two concurrent cleaning jobs can no longer contend for the same number.",
        "resolveJobSource() prefers ARTIFACTS while findArtifactSource() prefers VERSIONS. That asymmetry is deliberate and commented: the first answers what a NEW job should read (prefer the ledger so the silver result gets a real parent), the second answers where the bytes are (a legacy row must keep its historical answer).",
        "T05 DONE — the client transform libs are untouched and remain the fast local preview.",
        "T01 DEFERRED by user decision: the interactive imputation panel keeps its instant in-browser preview rather than a round trip per adjustment. Recorded, not hidden.",
        "SUITES — backend tsc clean beyond the 4 pre-existing auth spec errors; client tsc clean; backend jest dataset-version 17/17; eslint clean on every touched backend file.",
        "NOT DONE — no client wiring. datasetVersionService.clean/job/cancelJob/retryJob still have zero callers, so the DEAD-CODE finding that motivated this feature is NOT yet closed. See blocker.",
        "CLIENT WIRING DONE (this session, draft-first) — new services/dataset-draft.ts + hooks/dataset/use-dataset-draft-pipeline.ts give datasetDraftService.clean/job/cancelJob/retryJob real callers, closing V01 by grep. Step 3.2 Save Cleaned Tags now fires void applyClean(cleaningTags, draft) as a best-effort server sync (never blocks the pre-existing local Save flow), with Cancel/Retry surfaced inline only in the state they apply to.",
        "OP MAPPING VERIFIED, NOT GUESSED — read apps/python/services/cleaning_service.py:285-343 before writing lib/cleaning-op-mapper.ts. CLEANING_OPS is keyed by the browsers own CleaningMethod values by design (comment: \"so a saved recipe replays without translation\"); resolve_method() short-circuits to identity when op_type is already a registry key and method is omitted. So type: step.method needs no alias table; param/paramLow pass through by name via _PARAM_ALIASES. Pinned by lib/__tests__/cleaning-op-mapper.test.ts.",
        "BACKEND DRAFT API SURFACE ADDED — DatasetDraft lifecycle (create/get/abandon) + draft-scoped materialize/rows/clean/job/cancel/retry endpoints (apps/backend/src/api/v1/dataset-draft/**), sharing the SAME PreprocessingJobService singleton as the dataset path (DatasetVersionModule now exports it) rather than a second instance with a disjoint running map. Source-scoping guard (draft.sourceIds, mirroring dataset.sourceIds) sabotage-probed: removing it flips the refusal test to fail before being restored to green.",
        "BUG FOUND AND FIXED IN PASSING — retryJobService (dataset-scoped, pre-existing) copied only sourceVersionId on retry, dropping sourceArtifactId. Every DS-LAKE-005-era artifact-sourced job retried through it would have been created with NEITHER pointer set, which run() refuses to execute. Fixed; draft-scoped retryDraftJobService was written correctly from the start.",
        "NOT VERIFIED THIS SESSION — V03 (parity tolerance vs the browsers own prior computation) and V04 (live cancel-mid-job against minio-local) were not run live; the op-mapping is verified by reading the shared registry and by unit tests, not by an end-to-end parity run. Left pending rather than marked done.",
        "SUITES — backend: 25/25 new+existing dataset-draft/dataset-version jest tests green, tsc clean beyond the 4 pre-existing unrelated auth errors, eslint clean. client: tsc clean; new cleaning-op-mapper (4) + use-dataset-draft-pipeline (3) tests green; dataset-studio/dataset-version/step-5/preset-apply suites (48 tests) unaffected; the 3 pre-existing lib/__tests__/preprocessing.test.ts failures are the already-documented ones and did not grow. client eslint could not run — eslint-module-utils is missing from node_modules, a pre-existing environment gap unrelated to this session (confirmed absent from node_modules/.pnpm before any edit here).",
        "T01 CLOSED under a user-chosen hybrid (reversing the earlier full-defer): new POST /:id/artifacts/:artifactId/preview (backend, draft-scoped, mirrors the dataset path previewService, writes nothing) + datasetDraftService.preview (client) + requestFinalPreview/finalPreview on use-dataset-draft-pipeline.ts. Wired in step-3-2-imputation.tsx via a useEffect keyed on previewIndex === draft.length: EVERY intermediate scrubber position still computes purely locally (preprocessPipelines, unchanged) — only reaching the final step fires a 600ms-debounced server call, and leaving the final step resets to idle so a stale server preview can never sit next to a local preview of an earlier step. Backend: 7/7 dataset-draft jest (2 new), tsc/eslint clean. Client: 5/5 use-dataset-draft-pipeline tests (2 new, fake-timer debounce + idle-reset proof), tsc clean; the 6-suite/50-test regression set stayed green.",
        "V03 CLOSED, LIVE — added test_apply_operations_matches_client_output to apps/python/tests/test_parity.py, driving the ACTUAL runtime path (apply_operations, the function PreprocessingJobService really calls via /v1/preprocess/clean) with request-shaped operations built the same way lib/cleaning-op-mapper.ts::toCleaningOperations builds them, against every applicable golden fixture (19/23; 3 precleanse-engine fixtures stay deferred per existing project scope, 1 fixture is deliberately per-tag-divergent and not the wizards shape). All 19 pass within the established rel_tol 1e-9/abs_tol 1e-12. Full python suite: 302 passed, 30 skipped (skips are the pre-existing documented ones plus the 4 new, all legitimate), 0 failures.",
        "V04 CLOSED, LIVE — real minio-local + real running python (:8000) + a real PreprocessingJobService instance against the live dev Postgres. Seeded a genuine BRONZE artifact (5000 rows, 2 tags, 5% Bad cells) via ObjectStore.put_frame, then a 4-operation PreprocessingJob row. start() reached RUNNING with completedSteps=1 (genuinely mid-job, proven by polling, not assumed), then cancel() was called live: job settled to CANCELED with error \"Canceled by the user.\", zero new DatasetArtifact rows were created (only the pre-existing BRONZE remained under that runId) — proving no SILVER artifact leaks out of an interrupted job. Immediately after cancel, one straggler tmp object was still present (the in-flight connector step landing after the abort, exactly as documented in preprocessing-job.service.ts) — held the process open 33s and confirmed the scheduled straggler sweep cleared it: tmp prefix empty. This is not a hypothetical — the straggler was observed, then observed cleared. All seeded rows/objects deleted afterward; dev DB and MinIO left as found."
      ],
      "blocker_resolution": "RESOLVED by the artifact-first decision. The blocker was that create mode has no dataset id and no bronze artifact at Step 3. Under the decision there is no dataset id BY DESIGN, and stages address a draft namespace instead. T03 (the SILVER commit path) stays completed; T01/T02/T04 are re-scoped onto draft artifacts and depend on the open question above."
    },
    {
      "id": "DS-LAKE-006",
      "title": "Gold layer — port feature engineering and emit feature_spec.json",
      "priority": 6,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-005"
      ],
      "description": "Port applyFeatures, selectColumns and toModelReady from the browser to Python behind a golden-fixture gate, then produce a GOLD artifact with its feature specification stored beside it. This is the one genuinely new compute stage — DatasetVersionStage.FEATURE has existed in the enum since the first migration and no code has ever written it.",
      "tasks": [
        {
          "id": "DS-LAKE-006-T01",
          "title": "GATE FIRST: extend packages/parity-fixtures with cases covering derived features, column selection, and every scaler — deterministic, no Math.random, no wall clock",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-006-T02",
          "title": "apps/python/services/feature_service.py — port the transforms, matching the client bug-for-bug",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-006-T03",
          "title": "Extend the pytest parity harness to consume the new fixtures and fail loudly on drift",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-006-T04",
          "title": "Emit feature_spec.json: feature names, transformations, scaling, encoding, feature version, feature hash",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-006-T05",
          "title": "POST /v1/preprocess/features producing a GOLD artifact with parentArtifactId = the silver artifact",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-006-T06",
          "title": "Step 4 drives the server transform; the browser copy stays as the interactive preview",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "Every feature-engineering fixture passes as a real assertion, not a silent skip.",
        "A GOLD artifact is produced with parentArtifactId = silver and featureSpecKey populated.",
        "feature_spec.json contains feature names, transformations, scaling, encoding, feature version and feature hash.",
        "The feature hash is stable for an identical configuration and changes when any transform changes.",
        "Step 4's UI is unchanged."
      ],
      "verification": [
        {
          "id": "DS-LAKE-006-V01",
          "description": "Prove the gate bites: mutate one expected value in a fixture and confirm the suite FAILS, then restore. A gate that only reports green proves nothing — this is the exact failure mode caught in Slice 1's F0.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-006-V02",
          "description": "Sabotage probe the quirks specifically — rounding convention, scaler denominators, column ordering — on inputs where the alternatives provably differ.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-006-V03",
          "description": "The server's GOLD frame equals the browser's Step 4 output for the same recipe, within the established tolerance.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-006-V04",
          "description": "Existing apps/python suite does not regress.",
          "status": "pending"
        }
      ]
    },
    {
      "id": "DS-LAKE-007",
      "title": "Validation layer — quality gate and report",
      "priority": 7,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-006"
      ],
      "description": "The mandatory gate before a dataset may become official. Builds on assert_frame_shape, which is already the frame contract's enforcement primitive.",
      "tasks": [
        {
          "id": "DS-LAKE-007-T01",
          "title": "apps/python/services/validation_service.py — schema validation via assert_frame_shape, missing-value threshold, duplicate timestamp detection, feature consistency against feature_spec.json, statistical validation, completeness",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-007-T02",
          "title": "POST /v1/preprocess/validate — reads an artifact, writes validation_report.json, returns PASS/FAIL plus per-check detail",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-007-T03",
          "title": "Compute a quality score from the check results with a documented, testable formula",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-007-T04",
          "title": "Backend endpoint POST /:id/artifacts/:artifactId/validate with a zod-parsed response contract",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-007-T05",
          "title": "Configurable thresholds with stated defaults, so a FAIL is explainable rather than arbitrary",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "Validation writes a report and returns PASS or FAIL with a per-check breakdown.",
        "Validation writes no data artifact and mutates no frame.",
        "A FAIL names which check failed and by how much.",
        "The quality score is deterministic for a given artifact.",
        "Validation runs inside its HTTP timeout on a realistically large artifact."
      ],
      "verification": [
        {
          "id": "DS-LAKE-007-V01",
          "description": "Craft an artifact that violates each check in turn and confirm each one FAILS independently — a validator that passes everything is indistinguishable from no validator.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-007-V02",
          "description": "Assert validation writes nothing, using the NoWriteStore pattern already proven for preview.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-007-V03",
          "description": "Measure runtime on a multi-million-row artifact and record the headroom against the timeout.",
          "status": "pending"
        }
      ]
    },
    {
      "id": "DS-LAKE-008",
      "title": "Step 5 validation gate",
      "priority": 8,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-007"
      ],
      "description": "Surface the validation result inside the existing Review & Save step and block the Save button on FAIL. No step is added — this is the user-confirmed placement.",
      "tasks": [
        {
          "id": "DS-LAKE-008-T01",
          "title": "Validation report card in step-5-review-save.tsx: overall verdict, quality score, per-check rows",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-008-T02",
          "title": "Disable Save while validation is pending or FAILED, with the reason stated on the control rather than in a toast",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-008-T03",
          "title": "Re-validate when the recipe changes, so a stale PASS cannot authorise a save",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-008-T04",
          "title": "Follow docs/DESIGN_SYSTEM.md status colours — red/amber are reserved for workspace and plant status; data-quality UI uses neutral treatments",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "A FAILED validation makes Save unreachable, and the reason is visible without opening a dialog.",
        "A stale PASS cannot survive a recipe change.",
        "DW_TOTAL_STEPS is still 5 and wizard-shell labels are unchanged.",
        "The gate honours the reserved status-colour rule."
      ],
      "verification": [
        {
          "id": "DS-LAKE-008-V01",
          "description": "Component test: FAIL disables Save; PASS enables it; changing the recipe after a PASS re-disables it until revalidation.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-008-V02",
          "description": "Sabotage probe: remove the disabled condition and confirm the test fails.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-008-V03",
          "description": "Confirm no rows are written to Dataset, DatasetArtifact or DatasetVersion on the FAIL path.",
          "status": "pending"
        }
      ]
    },
    {
      "id": "DS-LAKE-009",
      "title": "Final artifact and the Save Dataset persistence boundary",
      "priority": 9,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-008"
      ],
      "description": "Save Dataset becomes the only operation that creates a DatasetVersion. It promotes the validated GOLD artifact into a FINAL artifact and commits the registry row atomically.",
      "tasks": [
        {
          "id": "DS-LAKE-009-T01",
          "title": "Produce a FINAL artifact from the validated gold artifact, with parentArtifactId = gold and validationKey populated",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-009-T02",
          "title": "Create the DatasetVersion in one prisma.$transaction: semanticVersion, artifactId, checksum, schemaVersion, rowCount, featureCount, qualityScore, status DRAFT, frozen lineage snapshot",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-009-T03",
          "title": "Read versionNumber inside the transaction so concurrent saves cannot collide on @@unique([datasetId, versionNumber])",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-009-T04",
          "title": "Refuse to save when validation has not passed — server-side, not only in the UI",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-009-T05",
          "title": "Assert no code path outside Save creates a DatasetVersion",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-009-T06",
          "title": "RELOCATED FROM DS-LAKE-002. Reshape DatasetVersion into the registry: add semanticVersion, artifactId, checksum, schemaVersion, qualityScore, status, lineage; drop stage, objectKey, parentVersionId, operations, columnStats",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-009-T07",
          "title": "RELOCATED FROM DS-LAKE-002. Migration 2 (reshaping) via pnpm db:migrate:dev, then pnpm --filter @softsensor/prisma build. Safe only here: this feature is where the last writer of the dropped columns disappears",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "Save Dataset is the only DatasetVersion write in the codebase.",
        "A failed save leaves no partial version and no duplicate.",
        "Save never creates a Model record.",
        "The final artifact is reachable from the version and carries a complete parent chain back to bronze.",
        "A save attempted without a PASS is refused by the server even if the client is bypassed."
      ],
      "verification": [
        {
          "id": "DS-LAKE-009-V01",
          "description": "Grep every prisma.datasetVersion.create call site and confirm exactly one, in the save path.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-009-V02",
          "description": "Force a mid-transaction failure and confirm zero DatasetVersion rows remain.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-009-V03",
          "description": "Full wizard run: exactly one DatasetVersion, and four DatasetArtifact rows sharing one runId with an intact parentArtifactId chain BRONZE→SILVER→GOLD→FINAL.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-009-V04",
          "description": "Call the save endpoint directly with an unvalidated artifact and confirm it is refused.",
          "status": "pending"
        }
      ]
    },
    {
      "id": "DS-LAKE-010",
      "title": "Dataset registry lifecycle, promotion and lineage",
      "priority": 10,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-009"
      ],
      "description": "Status transitions and a promotion path that moves metadata only. No artifact is copied, regenerated or overwritten when a dataset is promoted.",
      "tasks": [
        {
          "id": "DS-LAKE-010-T01",
          "title": "Promotion endpoint enforcing legal transitions DRAFT→VALIDATED→ACTIVE→DEPRECATED→ARCHIVED",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-010-T02",
          "title": "Reject illegal transitions with an AppException from @softsensor/common, never a raw HttpException",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-010-T03",
          "title": "Lineage endpoint returning the full artifact chain for a version, for audit and reproducibility",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-010-T04",
          "title": "Registry list/detail responses carry status, semanticVersion, checksum, qualityScore, rowCount, featureCount and owner",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-010-T05",
          "title": "Enforce at most one ACTIVE version per dataset",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "Promotion updates metadata only — objectKey and checksum are byte-identical before and after.",
        "Illegal transitions are refused.",
        "The registry returns metadata only and never dataset rows.",
        "Lineage resolves from any version back to its bronze artifact.",
        "A dataset never has two ACTIVE versions."
      ],
      "verification": [
        {
          "id": "DS-LAKE-010-V01",
          "description": "Record every artifact checksum before promotion and re-read after; assert equality. Also confirm the MinIO object count is unchanged.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-010-V02",
          "description": "Attempt ARCHIVED→ACTIVE and confirm refusal.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-010-V03",
          "description": "Inspect every registry response body and confirm no row payload leaks into it.",
          "status": "pending"
        }
      ]
    },
    {
      "id": "DS-LAKE-011",
      "title": "Loader seam — enqueue, retry, pluggable sink",
      "priority": 11,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-009"
      ],
      "description": "The asynchronous hand-off from a committed dataset to a serving layer. Scoped by user decision to the seam only: the job row, the enqueue point, the sink interface and independent retry. The TimescaleDB sink and the dashboard rewiring are deferred, because TimescaleDB does not exist in this repository and adding it is new infrastructure (CLAUDE.md §3).",
      "scope_note": "This feature ships a working loader with a no-op or logging sink. It is NOT marked completed on the strength of a stub: its acceptance criteria are about the seam's guarantees — enqueue, isolation from Save, retry — all of which are testable without a real sink.",
      "tasks": [
        {
          "id": "DS-LAKE-011-T01",
          "title": "LoaderJob model + status enum, mirroring the PreprocessingJob shape so the existing runner patterns transfer",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-011-T02",
          "title": "Sink interface with a single default implementation, so a Timescale sink can be added later without touching the enqueue path",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-011-T03",
          "title": "Enqueue after a successful Save, outside the save transaction",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-011-T04",
          "title": "Independent retry with attempt counting, and boot-sweep semantics matching PreprocessingJobService",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-011-T05",
          "title": "Job status endpoint so a future UI can report load progress",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "A loader failure never fails or rolls back Save.",
        "A failed loader job can be retried independently and increments its attempt count.",
        "The enqueue happens outside the save transaction.",
        "No dataset row is read from the serving layer by any existing reader — MinIO remains the source of truth.",
        "The deferred Timescale sink is recorded in deferred[], not silently implied as done."
      ],
      "verification": [
        {
          "id": "DS-LAKE-011-V01",
          "description": "Force the sink to throw, save a dataset, and confirm the dataset and its version are committed while the LoaderJob lands FAILED.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-011-V02",
          "description": "Retry the failed job and confirm a fresh attempt with attempts incremented, leaving the original row's history intact.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-011-V03",
          "description": "Kill the backend mid-load and confirm the boot sweep does not leave a job stuck RUNNING.",
          "status": "pending"
        }
      ]
    },
    {
      "id": "DS-LAKE-012",
      "title": "End-to-end verification of the Lakehouse invariants",
      "priority": 12,
      "status": "pending",
      "progress": 0,
      "dependencies": [
        "DS-LAKE-001",
        "DS-LAKE-002",
        "DS-LAKE-003",
        "DS-LAKE-004",
        "DS-LAKE-005",
        "DS-LAKE-006",
        "DS-LAKE-007",
        "DS-LAKE-008",
        "DS-LAKE-009",
        "DS-LAKE-010",
        "DS-LAKE-011"
      ],
      "description": "Prove every invariant the refactor claims, against a real run on live MinIO and Postgres — not by reading the code.",
      "tasks": [
        {
          "id": "DS-LAKE-012-T01",
          "title": "Full wizard run end to end against a real source, recording every artifact and row",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-012-T02",
          "title": "Reproducibility check — rebuild the dataset from MinIO artifacts alone, with Postgres consulted only for pointers",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-012-T03",
          "title": "Model flow regression against one pre-refactor dataset and one post-refactor dataset",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-012-T04",
          "title": "pnpm format, pnpm lint, pnpm check-types, pnpm build; python pytest; client vitest",
          "status": "pending",
          "progress": 0
        },
        {
          "id": "DS-LAKE-012-T05",
          "title": "Write the final report: architecture, storage, lifecycle, metadata, versioning, lineage, loader, MinIO, API and breaking changes, migration plan, validation results",
          "status": "pending",
          "progress": 0
        }
      ],
      "acceptance_criteria": [
        "Raw rows exist only in MinIO.",
        "Clean rows exist only in MinIO.",
        "Feature rows exist only in MinIO.",
        "The final dataset exists in MinIO.",
        "The Dataset Registry stores metadata only.",
        "A Dataset Version is created only after Save Dataset.",
        "Dataset promotion updates metadata only.",
        "The Loader retries independently of Save.",
        "The dataset is reproducible from MinIO alone.",
        "Model Training still works with no modification to apps/client/app/(default)/models/create/**."
      ],
      "verification": [
        {
          "id": "DS-LAKE-012-V01",
          "description": "Query every Postgres table and confirm no timeseries rows are stored anywhere.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-012-V02",
          "description": "Reconstruct the final dataset from its MinIO artifact and assert it matches the registry's recorded rowCount, featureCount and checksum.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-012-V03",
          "description": "git diff confirms zero changes under apps/client/app/(default)/models/create/.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-012-V04",
          "description": "Client suite failure count is compared against the recorded pre-existing baseline (18 failures, including 3 in lib/__tests__/preprocessing.test.ts on the legacy preprocess path) and has not grown.",
          "status": "pending"
        },
        {
          "id": "DS-LAKE-012-V05",
          "description": "pnpm build succeeds from a clean state, including pnpm --filter @softsensor/prisma build so dist/ is not stale.",
          "status": "pending"
        }
      ]
    }
  ],
  "deferred": [
    "TimescaleDB provisioning — image swap, extension, hypertables, retention and continuous aggregates. Nothing of this exists today and adding it is new infrastructure requiring an explicit decision (CLAUDE.md §3).",
    "The TimescaleDB loader sink. DS-LAKE-011 ships the seam and the interface; the concrete sink lands with the infrastructure decision.",
    "Dashboard and live-feed rewiring. Every dashboard figure today is either derived client-side from workspace/node rows or generated by lib/mock-readings — use-sensor-readings.ts:3-7 and use-pipeline-analytics.ts:43-58. services/readings.ts does not exist. This violates the 'no mock data' rule in docs/CODEBASE.md but predates this slice.",
    "Multi-source and CSV materialisation. materializeBlocker (use-dataset-version-rows.ts:100-106) refuses more than one source, CSV sources, and any non-PI type. Those datasets keep the synthetic fallback and its visible banner.",
    "The Model Creation persistence boundary. The Model row is created at Step 2 (use-model-training.ts:56 → use-model-commit.ts:81), not at Save Model. Tracked by docs/feature_list.json MODEL-FLOW-003 and MODEL-FLOW-007; explicitly out of scope here.",
    "Multi-replica job ownership. The in-process runner's boot sweep marks every RUNNING row FAILED, which is only correct with one backend instance. Escape hatch when that changes: SELECT ... FOR UPDATE SKIP LOCKED with an owner column — still no Redis.",
    "A tmp janitor. A hard-killed job's tmp intermediates are never reclaimed, and scheduleStragglerSweep's unref'd 30s timer does not survive a restart. Both leak storage, never wrong data.",
    "Deleting buildRawDataset and generateReadings. They remain the last resort for datasets with no artifact, and can only go once every dataset has a committed version.",
    "Parquet row-group pushdown. get_frame_slice and get_frame_head read the whole object then slice, so caps bound compute rather than I/O. Measured acceptable today; revisit when artifacts outgrow memory.",
    "docs/ARCHITECTURE.md and docs/CODEBASE.md do not describe apps/python, MinIO, object storage, the dataset models or background jobs at all. Updating them is worthwhile but is documentation work, not part of this refactor."
  ],
  "global_definition_of_done": [
    "MinIO is the single source of truth; Postgres stores metadata and pointers only.",
    "Every processing stage produces its own immutable artifact.",
    "No artifact is ever overwritten; a new run produces a new artifact.",
    "Every artifact records its parent, so lineage supports audit and reproduction.",
    "Every artifact carries a checksum computed at write time.",
    "Validation is mandatory and a FAIL blocks the commit.",
    "A Dataset Version is created only by Save Dataset.",
    "Intermediate stages never create a Dataset Version.",
    "Dataset rows are never stored in the Dataset Registry.",
    "Promotion changes metadata only; no artifact is copied or regenerated.",
    "Saving a Dataset never creates a Model and never starts training.",
    "The five-step wizard UX is unchanged.",
    "No use of any and no @ts-ignore.",
    "Errors are raised as AppException from @softsensor/common, never as raw Nest HTTP exceptions.",
    "Multi-step writes that must succeed together run in a Prisma transaction.",
    "Every feature is marked completed only after its acceptance criteria and verification items are actually verified, not merely implemented."
  ],
  "draft_architecture": {
    "entity": "DatasetDraft { id, name?, workspaceId, sourceIds[], status ACTIVE|SAVED|ABANDONED, currentArtifactId?, savedDatasetId?, createdById, createdAt, updatedAt } with artifacts[] and jobs[] back-relations.",
    "ownership_rule": "DatasetArtifact and PreprocessingJob each carry BOTH datasetId? and draftId?, with a CHECK constraint per table: (datasetId IS NOT NULL OR draftId IS NOT NULL).",
    "why_nullable": "READ THIS BEFORE CHANGING IT. The instruction \"do not make PreprocessingJob.datasetId nullable\" and the rule \"must reference either a Draft or a Dataset, never neither\" can only both hold if the COLUMN is nullable and a CHECK does the enforcing — a NOT NULL datasetId cannot coexist with a draft-owned job. The column is nullable; it is never unconstrained.",
    "both_set_is_legal": "Adoption at Save sets datasetId while KEEPING draftId, so a saved artifact stays traceable to the run that produced it. The CHECK permits both deliberately.",
    "minio_layout": "drafts/{draftId}/artifacts/{artifactId}/data.parquet, with the DS-LAKE-003 sidecars beside it. The earlier drafts/{userId}/... proposal is DROPPED: the DatasetDraft row carries createdById, so ownership is a database fact and does not need to be smuggled into the object key.",
    "source_scoping": "CRITICAL, carried over unchanged in intent. Draft materialize must scope the DataSource lookup to DatasetDraft.sourceIds, exactly as the dataset path scopes to Dataset.sourceIds (dataset-version.authorized.service.ts:224). The secret is decrypted server-side, so an unconstrained lookup would let a caller name an arbitrary source id and have the server connect with that source credentials.",
    "adoption_not_copying": "Save sets datasetId on the existing artifact rows and leaves objectKey untouched. No artifact is copied, rewritten or regenerated — required by the slice global_definition_of_done."
  }
}